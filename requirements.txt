# ResearchOps Agent 依赖
PyYAML>=6.0            # 读取 config / grid
python-dotenv>=1.0.0  # 读取 .env

# 第 2 层 LLM Agent
openai-agents>=0.0.3
openai>=1.30.0

# 第 3 层 Claude Code 风格 CLI
rich>=13.0.0
prompt_toolkit>=3.0.0

# 第 1 层（扫描/解析/汇总/异常/报告）只用标准库，无需额外安装。
# 后续可选：pandas（更强的结果聚合）、mlflow（实验追踪）。
