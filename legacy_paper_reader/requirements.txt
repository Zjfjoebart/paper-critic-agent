PyMuPDF>=1.23.0
sentence-transformers>=2.7.0
faiss-cpu>=1.8.0
openai-agents>=0.0.3
openai>=1.30.0
python-dotenv>=1.0.0
numpy>=1.24.0
questionary>=2.0.0          # /papers 方向键多选
rich>=13.0.0                # Claude Code 风格界面：彩色/Markdown 渲染
prompt_toolkit>=3.0.0       # Claude Code 风格界面：输入框/命令补全/历史

# arXiv 检索/下载仅用 Python 标准库（urllib + xml），无需额外安装。
# 扫描版 PDF 的 OCR 兜底为可选功能：需在系统层安装 Tesseract OCR
#   macOS:  brew install tesseract tesseract-lang
#   Ubuntu: sudo apt install tesseract-ocr tesseract-ocr-chi-sim
# 未安装时程序仍可正常运行，只是跳过扫描页。
